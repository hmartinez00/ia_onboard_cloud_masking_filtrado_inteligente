```text
Diagrama técnico vectorial en 2D, esquema de ingeniería de software y redes neuronales, diseño plano y minimalista, fondo blanco puro, paleta de colores estricta en azul cobalto (#0047AB) y gris técnico (#4A4A4A) con líneas finas en negro neutro. Sin sombras, sin gradientes, sin reflejos tridimensionales. Flujo de trabajo híbrido de arquitectura de aprendizaje profundo expuesto horizontalmente de izquierda a derecha. En el extremo izquierdo, el bloque de entrada ("Early Fusion") muestra una serie de rectángulos paralelos y desfasados que representan bandas espectrales convergiendo drásticamente a través de una forma de embudo geométrico hacia un único mapa de características denso. A continuación, el flujo se conecta a una serie de tres bloques consecutivos que representan las etapas convolucionales podadas de la CNN, ilustradas como rejillas de nodos interconectados por vectores finos; sobre estas capas se añade un lazo superior curvo que actúa como un mecanismo de atención ligera conectando la primera etapa con la última. Desde la salida de este bloque de atención, una flecha de flujo directo se conecta con un bloque jerárquico que representa un ensamble de Gradient Boosting, diseñado como una estructura ramificada de árboles de decisión shallow en paralelo. La salida final del árbol converge en una cuadrícula binaria simple de dos colores (Blanco y Azul cobalto) que representa la máscara de nubes resultante. Todo el texto y etiquetas alfabéticas están completamente ausentes, utilizando exclusivamente formas vectoriales nítidas y placeholders geométricos limpios. Orientación landscape técnica, estilo de publicación IEEE.

```

```text
2D technical vector diagram, neural network and software engineering schematic, flat design, minimalist, pure white background, strict color palette of cobalt blue (#0047AB) and technical gray (#4A4A4A) with crisp neutral black fine lines. No shadows, no gradients, no three-dimensional reflections. Hybrid deep learning architecture workflow laid out horizontally from left to right. On the far left, the "Early Fusion" input block features a series of stacked, offset parallel rectangles representing multi-spectral bands dynamically converging through a geometric funnel shape into a single dense feature map. Next, the flow connects to a series of three consecutive blocks representing pruned CNN convolutional stages, illustrated as node grids interconnected by fine vectors; a curved upper loop is overlaid across these layers as a lightweight attention mechanism connecting the first stage to the last. From the output of this attention block, a direct flow arrow connects to a hierarchical block representing a Gradient Boosting ensemble, styled as a branched parallel structure of shallow decision trees. The final output of the tree converges into a simple two-color (White and Cobalt Blue) binary grid representing the resulting cloud mask. Completely free of text, alphabetical labels, or typography, using only sharp vector shapes and clean geometric placeholders. Technical landscape orientation, pristine IEEE publication style.

```